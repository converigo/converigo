# Internal Link Normalization Report

## Summary

- Implemented format page internal link normalization using `InternalLinkService` as the primary source.
- Ensures all format page links are normalized to `/formats/{slug}`.
- Keeps converter links on existing tool routes generated by `InternalLinkService`.
- Uses central deduplication to avoid duplicate and self-referential links.

## Changes made

- Updated `app/routers/formats.py`:
  - Added `_internal_link_service()` helper
  - Replaced `_build_related_converters(normalized)` with `InternalLinkService.get_links_for_format(normalized)`
  - Exposed `related_formats`, `related_converters`, `related_knowledge`, and `related_hubs` to template context
- Updated `app/templates/pages/format_page.html`:
  - Switched `related_formats` rendering from `payload.related_formats` to `related_formats`
  - Continued using `related_converters` from the normalized internal link provider

## Validation

- Verified format page route rendering via `tests/test_formats_pages.py`
- Verified internal link generation via `tests/test_internal_link_service.py`
- Test result: `24 passed`

## Notes

- Converter logic was not changed.
- No service or template behavior outside format pages was modified.
- The existing `InternalLinkService` deduplication now serves format page link normalization consistently.
